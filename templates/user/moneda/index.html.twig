{% extends 'base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
<nav class="navbar navbar-expand navbar-dark bg-dark static-top">

  <img  href="{{path('dasboard_user')}}" src="{{ asset('assets/suenodoradopng.png') }}" class="img-fluid pull-xs-left" alt="..." width="20%">

  <button class="btn btn-link btn-sm text-white order-1 order-sm-0" id="sidebarToggle" href="#">
    <i class="fas fa-bars"></i>
  </button>

  <!-- Navbar Search -->
  <form class="d-none d-md-inline-block form-inline ml-auto mr-0 mr-md-3 my-2 my-md-0">

  </form>

  <!-- Navbar -->
  <ul class="navbar-nav ml-auto ml-md-0">

    <li class="nav-item dropdown no-arrow">
      <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        {{app.user.nombre}} <i class="fas fa-user-circle fa-fw"></i>
      </a>
      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
        <a class="dropdown-item" href="{{ path('edit_profile',{'id': app.user.id} )}}">Configuracion de perfil</a>
        <div class="dropdown-divider"></div>
        <a class="dropdown-item" href="#" data-toggle="modal" data-target="#logoutModal">Cerrar sesion</a>
      </div>
    </li>
  </ul>

</nav>

<div id="wrapper">

  <!-- Sidebar -->
  <ul class="sidebar navbar-nav">
    <li class="nav-item active">
      <a class="nav-link" href="{{path('dasboard_user')}}">
        <i class="fas fa-child"></i>
        <span>Perfil</span>
      </a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="#">
        <i class="fas fa-code-branch"></i>
        <span>Posición</span></a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="#">
        <i class="fas fa-envelope-square"></i>
        <span>Mensajes</span></a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="{{path('eventos_user')}}">
        <i class="fas fa-calendar-week"></i>
        <span>Eventos</span></a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="{{path('proyecto')}}">
        <i class="fas fa-question"></i>
        <span>Proyecto</span></a>
    </li>
  </ul>

  <div id="content-wrapper">

    <div class="container-fluid">
        {% if moneda.rango == 1 %}
        <form action="ejemplo.php" method="get">
        <h4>Añadir oros</h4>
        <select id="añadir_oro" class="custom-select"  multiple></select>
        <div>
            <br>
            <button class="btn btn-success" type="submit">Añadir</button>
        </div>

        </form>
        <br>
        {% endif %}
      <!-- DataTables Example -->
      <div class="card mb-3">
        <div class="card-header">
          <i class="fas fa-table"></i>Monedas</div>
        <div class="card-body">
          <div class="table-responsive">
            <table class="table table-bordered" id="Monedas" width="100%" cellspacing="0">
              <thead>
                <tr>
                <th width="3%">Orden</th>
                <th>Dueño</th>
                <th>invito a</th>
                <th>Rango</th>
                </tr>
              </thead>
              <tfoot>
              </tfoot>
              <tbody>

              </tbody>
            </table>
          </div>
        </div>
      </div>

    </div>
    <!-- /.container-fluid -->

    <!-- Sticky Footer -->
    <footer class="sticky-footer">
      <div class="container my-auto">
        <div class="copyright text-center my-auto">
          <span>Copyright © Your Website 2019</span>
        </div>
      </div>
    </footer>

  </div>
  <!-- /.content-wrapper -->

</div>
<!-- /#wrapper -->

<!-- Scroll to Top Button-->
<a class="scroll-to-top rounded" href="#page-top">
  <i class="fas fa-angle-up"></i>
</a>

<!-- Logout Modal-->
<div class="modal fade" id="logoutModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="exampleModalLabel">¿Seguro que desea cerrar sesion?</h5>
            <button class="close" type="button" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">×</span>
            </button>
          </div>
          <div class="modal-footer">
            <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button>
            <a class="btn btn-primary" href="{{path('app_logout')}}">Cerrar sesion</a>
          </div>
        </div>
    </div>
</div>

<div class="modal fade" id="borrarModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="exampleModalLabel">¿Seguro que desea borrar el usuario?</h5>
            <button class="close" type="button" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">×</span>
            </button>
          </div>
          <div id="BotonBorrar" class="modal-footer">
            <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button>
            <a class="btn btn-primary" href="">Borrar</a>
          </div>
        </div>
    </div>
</div>

<div class="modal fade" id="agregarOro" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="exampleModalLabel">Añadir oro a rubi</h5>
            <button class="close" type="button" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">×</span>
            </button>
          </div>
          <div class="modal-body">
              <form action="ejemplo.php" method="get">
              <h4>Añadir oros</h4>
              <select id="añadir_oro" class="custom-select" style='width:100%; height:100%' multiple></select>
              <div>
                  <br>
                  <button class="btn btn-success" type="submit">Añadir</button>
              </div>

              </form>
              <br>
        </div>
          <div id="agregarOroBoton" class="modal-footer">
            <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button>
            <a class="btn btn-primary" href="">Borrar</a>
          </div>
        </div>
    </div>
</div>

{% endblock %}
{% block javascripts %}
{{ parent() }}

<script type="text/javascript">

$(document).ready(function() {

        usersList();
    });

var arbol = [];
function usersList() {

    var misCabeceras = new Headers();
    var miInit = { method: 'POST',
           headers: misCabeceras,
           mode: 'cors',
           cache: 'default' };
  fetch('{{ path('list_monedas',{'id': moneda.id} )}}',miInit)
    .then(response => response.json())
    .then(myJson => {
        var table = $('#Monedas').DataTable({
        "order": [[ 0, 'desc' ]],
        "pageLength": 15,
         "destroy": true,
         rowCallback:function(row,data)
         {
           switch (data[3]) {
               case "Diamante":
                    $($(row).find("td")[3]).css("background-color","#6CAFFB");
                   break;
               case "Esmeralda":
                    $($(row).find("td")[3]).css("background-color","#6EED91");
                   break;
               case "Rubi":
                    $($(row).find("td")[3]).css("background-color","#F36F6F");
                   break;
               case "Oro":
                    $($(row).find("td")[3]).css("background-color","#D4B678");
                   break;
           }
         }
        });
     table.clear();
     //console.log(myJson);

     for (var i = 0; i < myJson.length; i++) {
         if (myJson[i].length > 1) {
             if (myJson[i].length == 2) {
                 var invto = "" + myJson[i][1].dueño.nombre;
             } else if (myJson[i].length == 3) {
                 var invto = "" + myJson[i][1].dueño.nombre + " y " + myJson[i][2].dueño.nombre;
             }

         }else {
             invto = "";
         }
         var rango = "";
         switch (myJson[i][0]['Padre'].rango) {
            case 0:
                rango = "Oro";
                break;
            case 1:
                var x = "modalAñadir("+myJson[i][0]['Padre'].id+")";
                var boton = `<button type='button' style='width:100%; height:100% id='ButtonEditar' onclick='${x}' class='btn btn-danger'><span class='hidden-xs'>Rubi</span></button>`;
                rango = boton;
                break;
            case 2:
                rango = "Esmeralda";
                break;
            case 3:
                rango = "Diamante";
                break;
         }

         table.row.add([
             myJson[i][0]['Padre'].rango,
             myJson[i][0]['Padre'].dueño.nombre,
             invto,
             rango
         ]);
         arbol.push(myJson[i][0]['Padre']);
     }


     table.draw();
    })
    .catch(function(err) {
       console.log(err);

    });
    var search = $('#añadir_oro').select2({
        maximumSelectionLength: 2
    });
}

function modalAñadir(id)
{
    var misCabeceras = new Headers();
    var miInit = { method: 'POST',
           headers: misCabeceras,
           mode: 'cors',
           cache: 'default' };
      fetch('{{ path('list_all_monedas') }}',miInit)
        .then(response => response.json())
        .then(monedasJson => {
            var select = document.getElementById("añadir_oro");
            for (var i = 0; i < monedasJson.length; i++) {
                var monedaIgual = arbol.find(moneda => moneda.id == monedasJson[i].id);
                if (monedaIgual == null) {
                    var newOption = new Option(monedasJson[i].dueño.nombre, monedasJson[i].id, false, false);

                    $('#añadir_oro').append(newOption).trigger('change');
                }
            }
        })
        .catch(function(err) {
           console.log(err);
        });
    $('#agregarOro').modal('show');
}

function modalBorrar(id)
{
    var uri  = "{{ path('delete_User',{'id': 'id'} )}}"
    uri = uri.replace('id', id);
    var content = `<button class='btn btn-secondary' type='button' data-dismiss='modal'>Cancel</button>
        <a class='btn btn-primary' href='${uri}'>Borrar</a>`;
    document.getElementById("BotonBorrar").innerHTML = content;
    $('#borrarModal').modal('show');
}
</script>

{% endblock %}
